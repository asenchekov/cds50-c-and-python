def lines(a, b):
    """Return lines in both a and b"""
    # Declaring the a and b lists
    lineA = []
    lineB = []
    for line in a:
        lineA.append(line)

    for line in b:
        lineB.append(line)

    return []


def sentences(a, b):
    """Return sentences in both a and b"""

    # TODO
    return []


def substrings(a, b, n):
    """Return substrings of length n in both a and b"""

    # TODO
    return []
